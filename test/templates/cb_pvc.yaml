apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-cloudbeaver-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
  volumeMode: Filesystem
  volumeName: test-cloudbeaver-pv
